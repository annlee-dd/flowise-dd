{
  "name": "${DD_SERVICE}",
  "version": "${DD_VERSION}",
  "description": "Flowise + LangChain with Datadog AI Observability",
  "scripts": {},
  "dependencies": {
    "dd-trace": "^5.0.0",
    "flowise": "^3.0.0",     
    "langchain": "^0.0.150", 
    "dotenv": "^16.0.3",
    "winston": "^3.11.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
